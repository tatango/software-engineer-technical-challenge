{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "dev": {
      "cache": false
    },
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["type-check", "lint", "format-check"]
    },
    "type-check": {},
    "lint": {},
    "format-check": {},
    "format": {},
    "migrate": {}
  }
}
